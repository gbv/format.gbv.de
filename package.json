{
  "name": "format.gbv.de",
  "scripts": {
    "validate": "./bin/metadata.js $(find pages -name '*.md') >/dev/null",
    "metadata": "mkdir -p pages/data/dumps; ./bin/metadata.js > pages/data/dumps/latest.ndjson"
  },
  "dependencies": {
    "ajv": "^6.10.0",
    "front-matter": "^3.0.2",
    "minimist": "^1.2.0"
  }
}
